<Window x:Class="TrainingDataViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrainingDataViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        Loaded="Window_Loaded">
    <StackPanel>
        <Button x:Name="directoryButton" Content="ディレクトリ選択" Click="DirectoryButton_Click"/>
        <TextBox x:Name="directoryPathBox" Text="ディレクトリパス"/>
        <Slider x:Name="slider"
            Minimum="0"
            Maximum="100"
            TickFrequency="1"
            SmallChange="1" 
            LargeChange="10"/>
        <TextBlock Text="{Binding Value, ElementName=slider}" />
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="text1" Text="ディレクトリパス"/>
            <TextBox x:Name="text2" Text="ディレクトリパス"/>
            <Image x:Name="Image"/>
        </StackPanel>
    </StackPanel>
</Window>
